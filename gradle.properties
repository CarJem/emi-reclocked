org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

## Minecraft / loader
minecraft_version=1.21.1
minecraft_version_range=[1.21.1,1.21.2)
neoforge_version=21.1.243
neoforge_version_range=[21.1.243,)
loader_version_range=[4,)

## Mod metadata
mod_id=emireclocked
mod_name=EMI Reclocked
mod_license=MIT
mod_version=0.1.0
mod_group_id=dev.reclocked.emireclocked
mod_authors=Cwall
mod_description=A behavior-preserving performance add-on for EMI and Reliable EMI (REMI): fixes redundant work in the tag-reload and recipe-bake phases, and adds per-creative-tab timing so world-load/data-reload freezes can be attributed to a specific mod.

## Dependency versions
# EMI has no normal Maven repository - Modrinth's maven-compatible endpoint is used instead,
# keyed by Modrinth's own opaque per-file version id (from `GET /v2/project/emi/version`), not
# EMI's own "1.1.24+1.21.1" version string. This id is EMI 1.1.24 for NeoForge 1.21.1, dated
# 2026-05-13. Re-resolve via the Modrinth API if EMI needs to be bumped.
emi_version=5sIPA1To
mixinextras_version=0.5.0
